// Dev commands from the TransformationDebugPanel to the director (applied next frame). Non-reactive.
export const transformationDev = {
  forceFinish: false, // fast-forward to completion (still switches form/collider)
  forceQuick: false, // restart the runner in quick mode
  reset: false, // restart the timeline from the beginning
};
