// Registry placeholder for effect types that render via another path (the v1 legacy types render through the
// bespoke EffectViz from snapshot.activeEffects); their registry entry exists only for editor metadata.
export const NullRenderer = () => null;
