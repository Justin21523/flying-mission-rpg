import { completeStage, getUnlockedStages, startCampaign, startStage, unlockStage } from './CampaignDirector';

export const StageProgressionRuntime = {
  startCampaign,
  startStage,
  completeStage,
  unlockStage,
  getUnlockedStages,
};
