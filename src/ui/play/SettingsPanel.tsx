// Batch 12 — the play-mode ⚙ Settings panel is now the tabbed panel in ui/settings. Re-exported here so the
// existing PlayToolbar mount keeps working (single panel, no duplicate settings source).
export { SettingsPanel } from '../settings/SettingsPanel';
