// POLI — sandbox mode. When true the world renders a BLANK canvas: only the ground, the player,
// and the camera, plus all Edit-Mode authoring layers (so you build everything yourself in Edit
// Mode). All pre-authored "background" content — kit sample entities and the POLI NPC / incident /
// traffic layers — is hidden. The code for those systems is kept intact and simply not rendered,
// so flipping this back to false restores them.
export const POLI_SANDBOX = true;
