import type { QualityPreset, QualityTier } from '../../types/game/quality';

// Batch 12 — seed quality presets. `high` ≈ today's look (so nothing regresses for High users); `ultra`
// pushes effects/AI budget up; `low`/`medium` trade fidelity for stability on mid desktops. `custom` is
// authored from a base (defaults to `high`) and stored as an override patch in graphicsSettingsStore.

export const QUALITY_PRESETS: Record<Exclude<QualityTier, 'custom'>, QualityPreset> = {
  low: {
    id: 'low',
    label: 'Low',
    renderLevel: 'low',
    shadowsEnabled: false,
    shadowMapSize: 512,
    postprocessingEnabled: false,
    bloomEnabled: false,
    colorGradingEnabled: false,
    particleQuality: 'low',
    weatherQuality: 'low',
    cloudQuality: 'low',
    transformationEffectQuality: 'low',
    maxActiveCharacters: 1,
    maxStandbyCharacters: 1,
    aiTickRateActive: 8,
    aiTickRateStandby: 2,
    objectPoolBudgetMultiplier: 0.6,
    dynamicFovEnabled: false,
    cameraShakeEnabled: false,
    speedLinesEnabled: false,
    airDistortionEnabled: false,
    targetFps: 60,
  },
  medium: {
    id: 'medium',
    label: 'Medium',
    renderLevel: 'medium',
    shadowsEnabled: true,
    shadowMapSize: 1024,
    postprocessingEnabled: true,
    bloomEnabled: true,
    colorGradingEnabled: true,
    particleQuality: 'medium',
    weatherQuality: 'medium',
    cloudQuality: 'medium',
    transformationEffectQuality: 'medium',
    maxActiveCharacters: 2,
    maxStandbyCharacters: 2,
    aiTickRateActive: 12,
    aiTickRateStandby: 3,
    objectPoolBudgetMultiplier: 1,
    dynamicFovEnabled: true,
    cameraShakeEnabled: true,
    speedLinesEnabled: true,
    airDistortionEnabled: false,
    targetFps: 60,
  },
  high: {
    id: 'high',
    label: 'High',
    renderLevel: 'high',
    shadowsEnabled: true,
    shadowMapSize: 2048,
    postprocessingEnabled: true,
    bloomEnabled: true,
    colorGradingEnabled: true,
    particleQuality: 'high',
    weatherQuality: 'high',
    cloudQuality: 'high',
    transformationEffectQuality: 'high',
    maxActiveCharacters: 3,
    maxStandbyCharacters: 2,
    aiTickRateActive: 15,
    aiTickRateStandby: 4,
    objectPoolBudgetMultiplier: 1.3,
    dynamicFovEnabled: true,
    cameraShakeEnabled: true,
    speedLinesEnabled: true,
    airDistortionEnabled: true,
    targetFps: 60,
  },
  ultra: {
    id: 'ultra',
    label: 'Ultra',
    renderLevel: 'high',
    shadowsEnabled: true,
    shadowMapSize: 4096,
    postprocessingEnabled: true,
    bloomEnabled: true,
    colorGradingEnabled: true,
    particleQuality: 'high',
    weatherQuality: 'high',
    cloudQuality: 'high',
    transformationEffectQuality: 'high',
    maxActiveCharacters: 3,
    maxStandbyCharacters: 3,
    aiTickRateActive: 20,
    aiTickRateStandby: 5,
    objectPoolBudgetMultiplier: 1.6,
    dynamicFovEnabled: true,
    cameraShakeEnabled: true,
    speedLinesEnabled: true,
    airDistortionEnabled: true,
    targetFps: 60,
  },
};

export const DEFAULT_QUALITY_TIER: QualityTier = 'medium';

/** Base used when authoring/resolving a 'custom' tier (a copy of High, then the user's patch on top). */
export const CUSTOM_PRESET_BASE: QualityPreset = { ...QUALITY_PRESETS.high, id: 'custom', label: 'Custom' };
